@model IEnumerable<Team1Centric940.Models.Gratitude>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.giverID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.giverID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.gratitudeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.gratitudeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.gratitudeId })
        </td>
    </tr>
}

</table>
